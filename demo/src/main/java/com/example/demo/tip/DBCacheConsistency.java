package com.example.demo.tip;

public class DBCacheConsistency {

    /**
     * 1、先更新缓存后更新数据库
     *      ---
     * 2、先更新数据库后更新缓存
     *
     * 3、先删除缓存后更新数据库（延时双删策略）
     *
     * 4、先更新数据库后删除缓存（缓存旁路策略）
     *
     *
     *
     */
}
